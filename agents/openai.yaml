# Codex UI metadata for the skill card and default invocation prompt.
interface:
  display_name: "XHS Feishu Delivery"
  short_description: "Generate Xiaohongshu packages and Feishu delivery cards"
  default_prompt: "Use $xhs-feishu-delivery to prepare a Xiaohongshu image-text package and send the complete delivery card to Feishu."
policy:
  allow_implicit_invocation: true
